{
  "name": "qiankun-demo",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "examples:install": "npm-run-all --serial install:*",
    "examples:start": "npm-run-all --parallel start:*",
    "install:main": "cd main-app && yarn",
    "start:main": "cd main-app && yarn serve",
    "install:vue": "cd micro-app-vue && yarn",
    "start:vue": "cd micro-app-vue && yarn serve"
  },
  "dependencies": {
    "qiankun": "^2.5.0"
  },
  "devDependencies": {
    "npm-run-all": "^4.1.5"
  }
}
